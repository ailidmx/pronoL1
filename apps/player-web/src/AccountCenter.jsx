import Profile from "./Profile.jsx"; import NotificationsPanel from "./NotificationsPanel.jsx"; import PasswordPanel from "./PasswordPanel.jsx"; import RulesPanel from "./RulesPanel.jsx";
const labels = { identity: "✏️ Pseudo et initiales", team: "⭐ Équipe de cœur", notifications: "🔔 Notifications", rules: "📖 Règles", password: "🔒 Mot de passe" };
export default function AccountCenter({ section, onSectionChange, onClose, onSignOut }) { return <div className="account-overlay" role="presentation" onMouseDown={(event) => { if (event.target === event.currentTarget) onClose(); }}><section className="account-dialog" role="dialog" aria-modal="true" aria-labelledby="account-title"><header><h1 id="account-title">Mon compte</h1><button type="button" onClick={onClose} aria-label="Fermer">×</button></header><nav aria-label="Réglages du compte">{Object.entries(labels).map(([key, label]) => <button type="button" className={section === key ? "active" : ""} onClick={() => onSectionChange(key)} key={key}>{label}</button>)}<button type="button" onClick={onSignOut}>🚪 Déconnexion</button></nav><div className="account-content">{section === "identity" ? <Profile section="identity" /> : null}{section === "team" ? <Profile section="team" /> : null}{section === "notifications" ? <NotificationsPanel /> : null}{section === "rules" ? <RulesPanel /> : null}{section === "password" ? <PasswordPanel /> : null}</div></section></div>; }
